#include <stdio.h>

/**
 * main - prints alpha in lower and then in upper case
 * Return: Always 0
 */
int main(void)
{
	char ch;
	char ch;

	for (ch = 'a'; ch <= 'z'; char++)
	{
		putchar(ch);
	}

	for (ch = 'A'; ch <= 'Z'; char++)
	{
		putchar(ch);
	}

	pitchar('\n');

	return (0);
}
